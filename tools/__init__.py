"""Operator diagnostics that run against a deployed bridge, not inside it.

Nothing here is imported by the bridge at runtime. These are read-only probes
for answering questions about the live rig — see `wled_soak` for the long-run
device/bridge sampler built for the DDP desync investigation.
"""
